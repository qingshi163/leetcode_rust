
fn find_median_single(nums: Vec<i32>, head: usize, tail: usize) -> f64 {

}

#[allow(dead_code)]
fn find_median_sorted_arrays(nums1: Vec<i32>, nums2: Vec<i32>) -> f64 {
    if nums.is_empty()
    let head1 = 0;
    let head2 = 0;
    let tail1 = nums1.len() - 1;
    let tail2 = nums2.len() - 1;
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_lc4() {
    }
}